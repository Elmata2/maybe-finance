# Specify the app name
name: maybe-finance

setup:
  addons:
    - plan: heroku-postgresql:essential-0
      as: DATABASE

build:
  docker:
    web: Dockerfile

run:
  web: bundle exec puma -C config/puma.rb
